# Project TODO

## Current Context

This project is a Sportybet booking codes aggregator. It scrapes booking codes from tipster sites and the Sportybet Code Hub API, validates them, caches them in memory, and serves them through a REST API with a frontend dashboard.

## What We Were Working On

- Add notes here about the last feature, bug, or investigation.
- Mention important files, such as `src/api/booking-codes-scraper.ts`, `src/api/social-codes-scraper.ts`, `src/api/routes/`, or `public/index.html`.
- Record any decisions already made so future sessions do not repeat the same analysis.

## Next Updates

- [ ] Describe the next code change to make.
- [ ] Note any scraper/API/dashboard behavior that needs updating.
- [ ] Run `pnpm build` after TypeScript changes.

## Notes For OpenCode

- Read this file before continuation work.
- Use `pnpm` commands.
- TypeScript is strict and ESM imports need `.js` extensions.
- No database is used; data is scraped live and cached in memory.
